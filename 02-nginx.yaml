- name: install and run nginx
  hosts: web
  tasks:
  - name: installing nginx package
    ansible.builtin.package:
      name: nginx
      state: present
  - name: run nginx
    ansible.builtin.package:
      name: nginx
      state: started
